import { PipesfilterPipe } from './pipesfilter.pipe';

describe('PipesfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
